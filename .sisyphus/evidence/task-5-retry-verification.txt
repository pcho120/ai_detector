Task 5: Intra-Round Retry with Varied Prompts — Verification Evidence
=====================================================================
Date: 2026-04-12 (updated)

Implementation Summary:
- Added `selectMoreDiverseRewrite(original, a, b)` helper in bulkRewrite.ts
  - Picks the rewrite with more unique words not in the original AND greater
    word-count difference from the original
- Single-sentence branch in executeBulkRewrite now has intra-round retry:
  1. Primary rewrite attempt with current promptVariationIndex
  2. If rewrite passes guardrails AND >8s remain before deadline:
     - Alt variation = (promptVariationIndex + 1) % BULK_PROMPT_VARIATIONS.length
     - Second attempt with alt variation
     - If alt also passes guardrails, selectMoreDiverseRewrite picks the better one
  3. Manual replacements (preserveReplacements) are never retried
- RETRY_DEADLINE_BUFFER_MS = 8_000 declared inside the retry block

Typecheck: PASS (tsc --noEmit — no errors)

Full test suite: PASS
  Test Files: 27 passed (27)
  Tests: 644 passed (644)
  Duration: ~6s

bulkRewrite.test.ts: 38/38 tests pass
  - "keeps original sentence indices stable across re-analysis rounds"
    - Tests retry with 2 isolated sentences, per-sentenceIndex mockImplementation
    - Verifies retry calls use altVariation (variation index 1)
    - Verifies selectMoreDiverseRewrite picks v2 rewrites
  - "should preserve manual replacements during rewrite"
    - Verifies manual replacements are NOT retried (calledIndices = [1, 1, 1, 1])
  - Includes tests for best-rewrite rollback
  - Includes tests for sentence index stability across re-analysis rounds

Changes made:
  - src/lib/bulk-rewrite/bulkRewrite.ts: Added selectMoreDiverseRewrite helper,
    intra-round retry logic in single-sentence branch with RETRY_DEADLINE_BUFFER_MS,
    altVariationIndex for second attempt
  - src/lib/bulk-rewrite/__tests__/bulkRewrite.test.ts: Updated mocks for retry
    slots, per-sentenceIndex mockImplementation for concurrent tests, fixed
    broken test at lines 926-937
