Task 1 TDD red check

Command: npx vitest run src/lib/api/__tests__/requestSettings.test.ts
Result: FAIL (intentional)

Failure observed in the new no-header defaults test:
- expected llmProvider to be 'openai'
- received 'anthropic'

This confirms the test suite now expects header-only resolution while production code still falls back to env vars.
