QA Scenario: Secrets are not present in client output
Tool: Bash
Steps: run production build; search output artifacts in .next/static for detector and suggestion env var names
Expected: no secret values or secret variable names are exposed in client bundle files
Date: 2026-03-31

--- Production build artifact secret scan ---

Command: grep -r "SAPLING_API_KEY" .next/static
Result: no matches (exit code 1 — grep found nothing)

Command: grep -r "COACHING_LLM_API_KEY" .next/static
Result: no matches (exit code 1 — grep found nothing)

--- Verification ---
No occurrences of SAPLING_API_KEY or COACHING_LLM_API_KEY found in .next/static/**

Secret access is server-only:
  src/app/api/analyze/route.ts: const apiKey = process.env.SAPLING_API_KEY;
  This file is a Next.js Route Handler (ƒ /api/analyze) — runs only on server.
  The /api/analyze route is listed as Dynamic (server-rendered) in build output.

Client bundle files inspected (.next/static/chunks/):
  chunks/255-38b49df12a94ee57.js   (46 kB)  — no secret env var names
  chunks/4bd1b696-c023c6e3521b1417.js (54.2 kB) — no secret env var names
  Other shared chunks              (1.89 kB) — no secret env var names

--- Additional: source code confirms server-only access ---
grep pattern: "SAPLING_API_KEY" in source (non-.next, non-.git, non-node_modules):
  ./src/app/api/analyze/route.ts: process.env.SAPLING_API_KEY  (server route — not bundled client-side)
  ./README.md: references SAPLING_API_KEY as an env var name in deployment docs only
  ./.env.example: SAPLING_API_KEY=your_sapling_api_key_here (example file, not a secret value)

Conclusion: no API keys or secret variable names present in client bundle output under .next/static.
